import { Lato, Roboto_Mono} from 'next/font/google';

export const lato = Lato({
    subsets: ['latin'],
    weight: ['400']
});

export const roboto = Roboto_Mono({
    subsets: ['latin'],
    weight:['400']
});